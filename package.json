{
  "name": "apollo-server-plugin-bugsnag",
  "version": "1.0.0",
  "description": "apollo-server plugin that send errors to bugsnag",
  "main": "dist/plugin.js",
  "types": "dist/plugin.d.ts",
  "license": "MIT",
  "devDependencies": {
    "apollo-server-plugin-base": "^0.7.0",
    "graphql": "^15.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^3.8.3"
  },
  "scripts": {
    "prepublishOnly": "rimraf dist && tsc -p . --outDir dist"
  },
  "dependencies": {
    "@bugsnag/js": "^6.5.2",
    "apollo-server-plugin-error": "^1.1.0",
    "tslib": "^1.11.1"
  }
}
